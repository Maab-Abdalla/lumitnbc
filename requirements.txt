flask>=3.0
flask-sqlalchemy>=3.1
werkzeug>=3.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3
shap>=0.43

# Production / deployment
gunicorn>=21.0
psycopg2-binary>=2.9

# LLM features (both optional, both use OpenAI; activate when OPENAI_API_KEY is set):
#   - Chatbot (LLM + RAG grounded in chatbot_kb.json)
#   - Patient "Your Personal Summary" card
# Without the key, the chatbot falls back to the offline keyword bot and the
# summary card is hidden. The app runs fine with no key set.
openai>=1.0
python-dotenv>=1.0
